/* Estilos CSS */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f3f3f3;
}

header {
    background-color: rgb(0, 0, 0);
    color: rgb(241, 234, 234);
    display: flex;
    justify-content: space-between; /* Adiciona espaço entre logo e botões */
    align-items: center; /* Alinha verticalmente ao centro */
    padding: 20px 20px; /* Adicionei padding no lado direito para separar a logo dos botões */
}

.logo {
    height: 80px;
    width: 80px;
    
}

.cta-buttons {
    display: flex;
}

.cta-button {
    margin-right: 5px;
    background-color: #0c1b70ee;
    color: #fff;
    text-decoration: none;
    border: none;
    border-radius: 6px;
    padding: 5px 15px;
    font-size: 16px;
    cursor: pointer;
}

.cta-button:hover {
    background-color: #898b9636;
}
